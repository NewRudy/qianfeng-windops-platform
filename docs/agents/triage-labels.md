# Triage Labels

Use this default vocabulary after GitHub Issues is created:

- `needs-triage`：需要判断是否值得做。
- `needs-info`：缺少用户、数据、验收或技术信息。
- `ready-for-agent`：已经足够明确，可以交给 AI 做一个垂直闭环。
- `ready-for-human`：需要人类产品或业务判断。
- `wontfix`：不进入参赛稳定平台。
